package com.syncrow.ui.theme

import androidx.compose.ui.graphics.Color

// Gemini High-Contrast Dark Palette
val DeepNavy = Color(0xFF0B162C) // Background
val BrightCyan = Color(0xFF4DFFFF) // Primary Action (JUST ROW)
val MutedNavy = Color(0xFF1B2B48) // Secondary Button Fill
val SlateGrey = Color(0xFFA0B1C5) // Utility Text (About/Quit)
val PureWhite = Color(0xFFFFFFFF) // Primary Text

// Keeping some Ocean Tech colors for Light Mode compatibility if needed
val LightGray = Color(0xFFF4F7FB)
val TextDark = Color(0xFF1A1C1E)
val Teal = Color(0xFF00BFA5)
val Purple = Color(0xFF9C27B0) // Kept for legacy/light mode secondary if needed
